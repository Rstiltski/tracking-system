# Core dependencies
streamlit>=1.33.0  # Required for st.fragment support (partial page updates)
pandas>=2.1.0  # Updated for performance improvements
requests>=2.31.0
streamlit-calendar>=0.6.0
bcrypt>=4.2.0  # Security patches applied
argon2-cffi>=23.1.0  # Argon2id for password hashing (recommended over bcrypt)
twilio>=9.0.0  # Updated to latest stable
sendgrid>=6.11.0  # Updated with bug fixes
stripe>=7.0.0  # Updated for latest Stripe API
schedule>=1.2.0
python-dotenv>=1.0.0  # For environment variable management

# Security - Phase 7: The Vault
cryptography>=41.0.0  # For Fernet encryption of PII at rest
PyNaCl>=1.5.0  # For Ed25519 signing and X25519/ChaCha20-Poly1305 encryption (Zero Trust architecture)

# Optional: Redis for nonce ledger (replay protection)
# redis>=5.0.0  # Uncomment for production deployments with high message volume

# Optional dependencies
reportlab>=4.0.0  # For PDF generation
Pillow>=10.2.0  # Image processing (security updates)

# Performance & Monitoring
cachetools>=5.3.0  # Advanced caching utilities
pydantic>=2.0.0
psutil>=5.9.0  # System and process monitoring
plotly>=5.18.0  # Interactive charts for monitoring dashboard

# AI Integration (optional - install only if needed)
langchain>=0.1.0
langchain-community>=0.0.20
langchain-openai>=0.0.5  # For OpenAI GPT models
langchain-anthropic>=0.1.0  # For Anthropic Claude models
langchain-google-genai>=0.0.5  # For Google AI Studio (Gemini) - FREE API!
openai>=1.0.0  # OpenAI API client
anthropic>=0.18.0  # Anthropic API client
google-genai>=0.1.0  # New Google Gen AI SDK (replaces google-generativeai)

# PostgreSQL support (optional - install only if using PostgreSQL)
# psycopg2-binary>=2.9.0  # PostgreSQL database adapter

# Development dependencies (optional)
pytest>=7.4.0  # Testing framework
pytest-cov>=4.1.0  # Code coverage
# black>=23.0.0  # Code formatter
# flake8>=6.1.0  # Linter

# Desktop Application Build (optional - for standalone executable)
pywebview>=4.0.0  # Native desktop window for standalone app
pyinstaller>=6.0.0  # Package app as standalone executable