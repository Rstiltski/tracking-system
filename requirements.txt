# Python dependencies for Veryfyn Tracking System
# Based on analysis of imports in Python files

# Standard library modules used (no need to install)
# sqlite3, json, datetime, pathlib, threading, uuid, logging, dataclasses, enum, typing, contextlib, os, sys, re, hashlib, time

# Third-party dependencies commonly used in similar projects
streamlit>=1.28.0
streamlit-aggrid>=1.0.0
sqlalchemy>=2.0.0
pyyaml>=6.0
requests>=2.31.0
streamlit-lottie>=0.0.5  # Phase 9: Gamevibe UI Overhaul (Animations)

# Phase 4: Notifications & Reminders System
apscheduler>=3.10.0      # Temporal orchestration for recurring reminders
pywebpush>=2.0.0         # Web Push API for browser notifications (optional)
jinja2>=3.1.0            # Template engine for notification messages

# Testing Dependencies (optional, for development)
# Install with: pip install -r requirements.txt --extra-index-url https://test.pypi.org/simple/
pytest>=7.4.0            # Test framework
pytest-cov>=4.1.0        # Coverage reporting
pytest-xdist>=3.5.0      # Parallel test execution
pytest-watch>=4.2.0      # Watch mode for auto-rerun
pytest-html>=4.1.0       # HTML test reports
